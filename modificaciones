//LENGUAJE C
//Proyecto final 

//librerias
#include <stdio.h>
#include <stdlib.h>
#include <iostream>
#include <string.h>
#include <time.h>

using namespace std;
//variables globales 

char p1[10], p2[10],p3[10],p4[10],p5[10];
char p11[10], p22[10],p33[10],p44[10],p55[10];

int ganar=0;
char verificador;
int intentos=10;
char letra;

//funciones 

void verificarPalabra(void);
void verificarletra(void);
void intento(void);
//funcion principal
int main()
{
	
	//variables locales
	 char nombre[100], nombre2[100];
	 int palabra,long1,long2,long3,long4,long5;
	 int elegir;
	 
	 
	 
	 //bienvenida y eleecion de palabras ocultas
	 cout<<"Hola bienvenido a ADIVINA LA PALABRA "<<endl;
	 cout<< "Este es un juego para jugar en pareja: uno escribe las palabras y el otro las adivina"<<endl;
	 cout<<"Escribe tu nombre: "<<endl;
	 cin.getline(nombre,100,'\n');//guarda el nombre con espacios en la variable
	 cout<<"hola "<<nombre<<" escribe 5 palabras diferentes:"<<endl;
	 cout<<"1. ";
	 cin>>p1;
    cout<<"2 ";
	 cin>>p2;
	 cout<<"3 ";
	 cin>>p3;
	 cout<<"4 ";
	 cin>>p4;
	 cout<<"5 ";
	 cin>>p5;
	 cout<<"Por favor llena de guiones los espacios correspondientes segun las palabras,el turno del otro jugador inciara justo cuando termines "<<endl;
     cout<<"1. ";
	 cin>>p11;
     cout<<"2 ";
	 cin>>p22;
	 cout<<"3 ";
	 cin>>p33;
	 cout<<"4 ";
	 cin>>p44;
	 cout<<"5 ";
	 cin>>p55;
	 cout<<"Gracias, chao  "<<nombre<<endl; 
	 system("cls");
	 
	 //tamaño de las palabras 
	 long1= strlen(p1);
	 long2= strlen(p2);
	 long3= strlen(p3);
	 long4= strlen(p4);
	 long5= strlen(p5);
	 
	 //eleccion de palabra
	 getchar();
	 cout<<"Escribe tu nombre"<<endl;
	 cin.getline(nombre2,100,'\n');//guarda el nombre con espacios en la variable
	 
	 cout<<" Hola  "<<nombre2 <<"  escoje un numero para adivinar la palabra"<<endl<<"1."<<endl<<"2."<<endl<<"3."<<endl<<"4."<<endl<<"5"<<endl;
	cin>>palabra;


    switch (palabra)
	 {
	 	case 1:
	 		{
            
				while(ganar!=long1)
             	{
				   // void verificarPalabra();  void intento();
			        cout<<"palabra oculta: "<<p11<<endl;
		        	cout<<"escriba una letra"<<endl;
			        cin>>letra;
		           //validamos letra
		           for(int i=0;i<long1;i++)
		           {
		            	if(p1[i]==letra)
		             	{
		           		   p11[i]=letra;
					    }
				   }
				   getchar();
				   intentos-=1;
				   printf("intentos restantes: %d \n", intentos);  
				
				   //validamos palabra 
				    for(int j=0;j<long1;j++)
                    {
 			            if(p1[j]==p11[j])
 		                {
 			            	ganar+=1;
		                }
					}
				
					if(ganar==long1)
					{
						system("color 20");
						cout<<"palabra oculta: "<<p11<<endl;
						cout<<"Muy bien has descubierto la palabra  "<<endl;
						FILE *archivo=fopen("intento2.html", "a");
						fprintf(archivo, "\n creador palabra:%s - jugador:%s - palabra secreta:%s - intentos restantes:%d - resultado:adivinó", nombre,nombre2,p1,intentos);
						fclose(archivo);
						system("intento2.html");
						
						break;
					}
					else
					{
						ganar=0;
					}

		        	if(intentos<1)
		        	{
		            	system("color 40");
						cout<<"Lo siento, has llegado al numero limite de intentos"<<endl;
						FILE *archivo=fopen("intento2.html", "a");
						fprintf(archivo, "\n creador palabra:%s - jugador:%s - palabra secreta:%s - intentos restantes:%d - resultado:no adivinó", nombre,nombre2,p1,intentos);
						fclose(archivo);
						system("intento2.html");
		            	break;
		        	}
				}
             	break;
			 }
		case 2:
	 		{
	 			cout<<"hola";
	 			break;
			 }	
		case 3:
	 		{
	 		   cout<<"hola"	;
	 		   break;
			 }	  
	 }
	 
	 
	 
return 0;
}
 
